<div class="container container-profile">

  <div class="col-md-3">
    <%= render 'layouts/user_bar' %>
  </div>
  <div class="col-md-9">
    <%= render 'layouts/tabs' %>

    <div class="feed">

        <% @orgs.each do |org| %>
          <li>
            <h3><%= link_to "#{org[:login]}", orgs_path %></h3>
            <hr>
          </li>
        <% end %>
      </div>
  </div>
</div>
